#Language
language: php

#Version number
php:
  - 5.4
#  - 5.5
#  - 5.6
 # - 7.0
 
env:
  - FOO=Bar
  
build:
  pre_ci:
    - docker build -t=shippabledocker/shiphitchcock1_sample_phptest .

  pre_ci_boot:
    image_name: shippabledocker/shiphitchcock1_sample_phptest
    image_tag: latest
    pull: false
    
  ci:
    - echo "$FOO"
    #- php --version 
   # - ps -eax
   # - ls -al 

  post_ci:
    - docker push shippabledocker/shiphitchcock1_sample_phptest

integrations:
  hub:
    - integrationName: ship-docker
      type: docker
