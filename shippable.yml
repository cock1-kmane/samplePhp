#Language 
language: php

#Version number
php:
  - 5.4
#  - 5.5
 # - 5.6
 # - 7.0
  
build:
  ci:
    - php --version    

  post_ci:
    - docker build -t=shiphitchcock1/betaci_php_pipeline:$BRANCH.$BUILD_NUMBER .
    - docker push shiphitchcock1/betaci_php_pipeline:$BRANCH.$BUILD_NUMBER

integrations:
  hub: 
    - integrationName: shiphitchcock1-hub
      type: docker 
